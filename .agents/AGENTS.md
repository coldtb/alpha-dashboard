# Workspace Rules

- **Code & Configuration Freeze:** The bot's strategy logic, indicators, and configuration parameters (e.g., watchlist, minScore, cooldownHours, positionSizeFactor, etc.) are strictly frozen. Do NOT change any settings or code in `api/bot.js` or `config.json` unless an active bug, error, or system issue is identified.
